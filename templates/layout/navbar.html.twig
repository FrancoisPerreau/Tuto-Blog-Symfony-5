<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('home') }}">THE BLOG</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('home') }}">Accueil
                        <span class="sr-only">(current)</span>
                    </a>
                </li>
            </ul>
            {% if app.user %}
                <a class="btn btn-danger mx-1" href="{{ path('security_logout') }}">Déconnexion</a>
                {% if is_granted('ROLE_ADMIN') %}
                    <a class="btn btn-warning mx-1" href="{{ path('admin') }}">Administration</a>
                {% endif %}
            {% else %}
                <a class="btn btn-danger mx-1" href="{{ path('security_register') }}">Inscription</a>
                <a class="btn btn-danger mx-1" href="{{ path('security_login') }}">Connexion</a>
            {% endif %}
            
        </div>
    </div>
</nav>